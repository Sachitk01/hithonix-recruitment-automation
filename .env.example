# Copy to .env and update values or replace with gcp-secret://<secret-name>
# Any variable set to gcp-secret://<secret-name> will be fetched from Google Secret Manager
# when GOOGLE_CLOUD_PROJECT or GCP_SECRET_PROJECT is provided.

# --- Core application secrets ---
OPENAI_API_KEY=gcp-secret://openai-api-key

# --- Slack (Riva L1) ---
SLACK_RIVA_BOT_TOKEN=gcp-secret://slack-riva-bot-token
SLACK_RIVA_SIGNING_SECRET=gcp-secret://slack-riva-signing-secret
SLACK_RIVA_DEFAULT_CHANNEL_ID=C12345678
SLACK_RIVA_BOT_USER_ID=URIVABOT123

# --- Slack (Arjun L2) ---
SLACK_ARJUN_BOT_TOKEN=gcp-secret://slack-arjun-bot-token
SLACK_ARJUN_SIGNING_SECRET=gcp-secret://slack-arjun-signing-secret
SLACK_ARJUN_DEFAULT_CHANNEL_ID=C12345678
SLACK_ARJUN_BOT_USER_ID=UARJUNBOT123

# --- Google Drive service account ---
RIVA_SA_JSON= # optional local path to service-account.json
RIVA_SA_JSON_CONTENT=gcp-secret://drive-service-account-json
GOOGLE_APPLICATION_CREDENTIALS=infrastructure/service_account.json
RECRUITER_SHEET_FILE_ID=1ZqNfOsyyNs5wBSTU8Xm-IxZAF1X27SpJsDwyaAwvpj4

# --- Scheduler + runtime ---
ENABLE_JOB_SCHEDULER=true
PORT=8000

# --- Talent Intelligence Memory Layer ---
MEMORY_ENABLED=true
MEMORY_SCOPE=full
MEMORY_DB_URL=sqlite:///./talent_memory.db

# --- Cloud + deployment ---
GOOGLE_CLOUD_PROJECT=your-gcp-project
GCP_SECRET_PROJECT= # overrides GOOGLE_CLOUD_PROJECT for Secret Manager lookups if set
GCP_REGION=us-central1
CLOUD_RUN_SERVICE=hithonix-recruitment-api

# --- Slack dev helpers ---
NGROK_AUTHTOKEN=
NGROK_DOMAIN= # optional reserved domain like your-subdomain.ngrok.app

# --- Other integrations ---
SHEET_ID=15iFeJ7kkM9_-29VC11DLhGx8NvZNQVkMYqpajeh3rho

# --- Notes ---
# 1. Prefer storing real secrets in Google Secret Manager and set the env variable to gcp-secret://<secret-name>.
# 2. When running on Cloud Run, mount secrets via Secret Manager or Cloud Secret volumes and leave these blanks.
# 3. For local development, run `cp .env.example .env` and fill in values or run `gcloud secrets versions access`.
